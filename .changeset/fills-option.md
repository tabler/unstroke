---
"unstroke": minor
---

The `includeFills` option is renamed to `fills`, matching the CLI's `--no-fills` and the other boolean option `curves` / `--no-curves`. `includeFills` still works and is marked deprecated.
