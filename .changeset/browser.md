---
"unstroke": patch
---

Browser support is stated and tested: the library entry has no Node dependencies, and the test suite runs it in a DOM environment. Only the CLI and the `unstroke/optimize` SVGO pass need Node.
