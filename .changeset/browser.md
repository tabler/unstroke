---
"unstroke": patch
---

Browser support is now stated and tested: the library entry has no Node dependencies and a test runs it in a DOM environment. Only the CLI and the `unstroke/optimize` SVGO pass need Node.
