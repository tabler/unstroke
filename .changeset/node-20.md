---
"unstroke": major
---

Node 20 or newer is required (`engines.node` was `>=18`). Node 18 reached end of life and is no longer tested; CI runs on Node 24. Nothing else changes in this release, so 0.2.x users on Node 20+ can upgrade without touching their code.
