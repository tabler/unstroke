---
"unstroke": major
---

Node 20 or newer is required; `engines.node` was `>=18`. Node 18 reached end of life and was never in the test matrix. CI now runs on Node 24, with the test suite also run on Node 20 as the lowest supported version.
